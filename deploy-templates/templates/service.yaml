apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet-expose.fullname" . }}
  labels:
    {{- include "dotnet-expose.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dotnet-expose.selectorLabels" . | nindent 4 }}
